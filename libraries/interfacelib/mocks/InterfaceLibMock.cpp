#include "InterfaceLibMock.hpp"

// Dummy source file to satisfy STATIC library requirements if needed,
// though usually header-only mocks can be tricky with static libs unless
// there's at least one compilation unit.
